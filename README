A Hadoop Job scheduler that assign task in a batch and round robin manner.

At each heartbeat,the scheduler go through each job and assign it to tasktracker.
Jobs maintaind an order as when they were submited.


